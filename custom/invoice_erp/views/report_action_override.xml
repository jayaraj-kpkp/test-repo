<odoo>
    <record id="action_report_invoice_custom" model="ir.actions.report">
        <field name="name">Custom Invoice</field>
        <field name="model">account.move</field> <!-- ✅ THIS IS CRITICAL -->
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">invoice_erp.report_invoice_document_custom</field>
        <field name="report_file">invoice_erp.report_invoice_document_custom</field>
        <field name="print_report_name">'Custom Invoice - %s' % (object.name)</field>
        <field name="binding_model_id" ref="account.model_account_move"/> <!-- Optional: Binds to print dropdown -->
        <field name="binding_type">report</field>
        <field name="report_name">invoice_erp.report_invoice_document_custom</field>
        <field name="report_file">invoice_erp.report_invoice_document_custom</field>
        <field name="invoice_report" eval="True"/> <!-- ✅ Optional: If you want to make it the default -->
    </record>
</odoo>
